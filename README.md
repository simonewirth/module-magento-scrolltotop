# Swirth Scroll To Top

## Description

This module makes a scroll to top button.

## Installation

> - `composer require swirth/scroll-to-top`
> - `bin/magento setup:upgrade `
> - Navigate to "Shop->Configuration->Swirth->ScrollToTop" and configure the options.
> - Clear the cache.
